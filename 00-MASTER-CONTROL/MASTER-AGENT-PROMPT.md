# MASTER PROMPT — COPY THIS TO YOUR CODING AGENT

You are building the OMARI Technologies Four-Year IAM Career Lab on the student's Windows 11 host.

Read every file under `00-MASTER-CONTROL`, `01-LAB-ARCHITECTURE`, and `02-OLLAMA-INSTRUCTOR` before modifying the lab.

Build ONE evolving enterprise.

NON-NEGOTIABLE:
- Real bootable VMs, not simulated desktops.
- Real Windows Server DC01 with AD DS and DNS.
- Real Windows 11 HD01 joined to the domain.
- Normal Windows login/desktop.
- Real users, groups, OUs and GPOs.
- Private Hyper-V networking.
- Validation after every major change.
- No fabricated success.
- No secrets in source control.
- Authorized systems only.
- Ollama is the local tutor.
- The tutor must use progressive hints and not immediately reveal answers.
- Year 1 is unlocked first.
- Years 2–4 are architected now but remain gated until prior assessments pass.

Use the supplied roadmap and phase files as the source of truth.

Your first action is NOT to start changing the system. Inspect:
1. host OS
2. virtualization
3. Hyper-V
4. RAM
5. disk
6. available installation media
7. existing VMs/switches
8. existing checkpoints
9. Ollama availability

Then provide a readiness report.

After approval or where the requested build is clearly safe and reversible, create the environment in small validated steps.

At the end of every session update the checkpoint and tell the student exactly where to resume.

Do not claim a commercial IAM product has been installed unless it was actually installed through a legitimate authorized mechanism. If unavailable, implement the same learning concept with a lawful lab equivalent.
